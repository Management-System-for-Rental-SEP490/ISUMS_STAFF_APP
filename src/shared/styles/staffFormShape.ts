/**
 * Bo góc / hình dạng điều khiển form staff — dùng chung modal bảo trì,
 * ghi nhận sửa chữa và DropdownBox để UI đồng nhất.
 */
export const staffFormShape = {
  /** Ô nhập, nút, hàng draft, panel dropdown */
  radiusControl: 14,
  /** Thẻ modal / section card */
  radiusSurface: 20,
  /** Chip lọc (tầng, v.v.) */
  radiusChip: 999,
} as const;
